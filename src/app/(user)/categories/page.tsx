import React from 'react';

export const metadata = {
    title: "Categories - TaskUp",
};

const Categories = () => {
    return (
        <div>
            asdas
        </div>
    );
};

export default Categories;